apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegisos-ops.fullname" . }}-console-service
  labels:
    {{- include "aegisos-ops.labels" . | nindent 4 }}
    app: console
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.consolePort }}
      targetPort: {{ .Values.service.consolePort }}
      protocol: TCP
      name: http
  selector:
    {{- include "aegisos-ops.selectorLabels" . | nindent 4 }}
    app: console
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegisos-ops.fullname" . }}-litellm-service
  labels:
    {{- include "aegisos-ops.labels" . | nindent 4 }}
    app: litellm
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.litellmPort }}
      targetPort: {{ .Values.service.litellmPort }}
      protocol: TCP
      name: api
  selector:
    {{- include "aegisos-ops.selectorLabels" . | nindent 4 }}
    app: litellm
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegisos-ops.fullname" . }}-ollama-service
  labels:
    {{- include "aegisos-ops.labels" . | nindent 4 }}
    app: ollama
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.ollamaPort }}
      targetPort: {{ .Values.service.ollamaPort }}
      protocol: TCP
      name: api
  selector:
    {{- include "aegisos-ops.selectorLabels" . | nindent 4 }}
    app: ollama
