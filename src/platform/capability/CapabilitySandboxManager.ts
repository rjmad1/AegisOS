import { ICapabilitySandbox, CapabilityMetadata } from "./types";

export class CapabilitySandboxManager implements ICapabilitySandbox {
  public async applyPolicy(metadata: CapabilityMetadata): Promise<void> {
    // Stub
  }
}
