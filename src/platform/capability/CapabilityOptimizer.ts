import { ICapabilityOptimizer, ResourceProfile } from "./types";

export class CapabilityOptimizer implements ICapabilityOptimizer {
  public async optimize(profile: ResourceProfile): Promise<void> {
    // Stub
  }
}
